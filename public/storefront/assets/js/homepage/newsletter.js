(function ($) { if (!$ || !$('[data-newsletter]').length) return; $('[data-newsletter]').on('submit', function (event) { event.preventDefault(); const $form = $(this); const $message = $form.siblings('.form-message'); if (this.checkValidity()) { $message.text('ایمیل شما با موفقیت ثبت شد.'); this.reset(); } else { $message.text('لطفاً یک ایمیل معتبر وارد کنید.'); } }); }(window.jQuery));
