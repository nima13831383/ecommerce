(function ($) { if (!$ || !$('.hero-slider, [data-component="hero-slider"]').length) return; const $root = $('[data-component="hero-slider"]'); const $track = $root.find('.slider-track'); const $slides = $root.find('.slider-slide'); const $dots = $root.find('.slider-dot'); let current = 0; function goTo(index) { current = (index + $slides.length) % $slides.length; $track.css('transform', 'translateX(-' + (current * 100) + '%)'); $dots.removeClass('is-active').attr('aria-selected', 'false').eq(current).addClass('is-active').attr('aria-selected', 'true'); } $root.on('click', '[data-action="prev"]', function () { goTo(current - 1); }).on('click', '[data-action="next"]', function () { goTo(current + 1); }).on('click', '[data-slide]', function () { goTo(Number($(this).data('slide'))); }).on('keydown', function (event) { if (event.key === 'ArrowLeft') goTo(current + 1); if (event.key === 'ArrowRight') goTo(current - 1); }); }(window.jQuery));
